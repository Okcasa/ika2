'use client';

import { DashboardView } from '@/components/dashboard-view';

export default function ShopViewPage() {
    // Reusing DashboardView for now as it serves as the shop interface
    return <DashboardView />;
}
